# Dev/test-only dependencies (NOT installed in the production Docker image).
#   pip install -r requirements-dev.txt
# Run the suite from backend/:  python -m pytest tests/ -q
-r requirements.txt
pytest>=8.0
httpx>=0.28.1          # FastAPI TestClient transport
pytest-xdist>=3.5      # -n auto --dist loadfile; see backend/pytest.ini
