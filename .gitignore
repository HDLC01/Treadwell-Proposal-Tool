# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Environment variables
.env
.env.local
.env.*.local
.env.staging

# IDE
.idea/
.vscode/
*.swp
*.swo

# Personal Claude Code settings — hooks, local tool config. Shared agent/skill
# definitions under .claude/agents/ and .claude/skills/ ARE committed; this is
# the per-developer overlay (e.g. a design-checking hook from installing the
# Impeccable skill locally) and should not silently start running in everyone
# else's session just because one machine has it installed.
.claude/settings.local.json
.codex/settings.local.json

# Impeccable's own compiled engine — a per-platform binary (Windows/Mac/Linux
# each get a different one) plus its supporting scripts, all regenerated by
# `npx impeccable install` on whichever machine needs it. The skill's actual
# knowledge (reference/*.md, SKILL.md) is committed above; the engine binary
# is not something to ship in git for teammates on a different OS to inherit.
.claude/skills/impeccable/scripts/
.github/skills/impeccable/scripts/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Temporary
*.tmp
*.bak
*~

# Test artefacts
test_output_*.xlsx
test_output_*.docx
.playwright-mcp/
/estimate-2systems.png
/intake-redesign.png
/local-login.png
/vps-*.png

# Local SQLite drafts (runtime data — production uses a Docker volume)
backend/data/

# Kyle's trailing-12 workbook: real win rates and revenue by trade. This repo is public.
# A sanitized copy of its LAYOUT lives at backend/tests/fixtures/trailing12_layout.xlsx.
Trailing 12TH MONTH.xlsx

# ── Customer material and working files that live in the repo root ───────────
# THIS REPO IS PUBLIC. Real customer documents get dropped in the working
# directory while a bid is being built — a project info sheet, a deposit
# invoice, an install form, a lock-up drawing. None of it belongs in git, and
# none of it was ignored until now: on 2026-08-19 a `git add -A` here staged
# sixteen of them, and the `git reset --hard` that followed deleted the lot.
# They were recovered from the orphaned commit, but the only durable fix is for
# git to stop seeing them at all. Ignoring them also keeps `git status` legible,
# which is what makes an accidental `add -A` less tempting in the first place.
#
# Scoped to the repo ROOT (leading /) so a genuine fixture committed under
# backend/tests/fixtures/ is unaffected.
/*.docx
/*.xlsx
/*.pdf
/*.png
/*.jpg
/*.eml
/*.msg
# Drafted outbound messages, written here for review before they are sent.
/email-*.txt
